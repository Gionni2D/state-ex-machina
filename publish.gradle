def moduleId  = ext.artifactId

publishing {
    publications {
        aar(MavenPublication) {
            groupId 'state.ex.machina'
            artifactId moduleId
            version '0.1.0'

            afterEvaluate {
                from components.release
            }
        }
    }
}
